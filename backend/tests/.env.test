# Test Environment
ENV=testing
SECRET_KEY=test-secret-key-not-for-production

# Test Upload Configuration
# Note: This path must exist before running tests
UPLOAD_FOLDER=/workspaces/FAIRDatabase/backend/tests/uploads

# Supabase Configuration (use same local instance)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=sb_secret_N7UND0UgjKTVK-Uodkm0Hg_xSvEMPvz

# PostgreSQL Configuration (same local DB, tests use transactions/rollback)
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=54322
POSTGRES_USER=postgres
POSTGRES_SECRET=postgres
POSTGRES_DB_NAME=postgres
